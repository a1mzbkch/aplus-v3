# 🚀 Деплой на Vercel

## Быстрый старт

### Вариант 1: Через веб-интерфейс Vercel (Рекомендуется)

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New Project"**
3. Импортируйте репозиторий: `https://github.com/a1mzbkch/aplus`
4. Настройки проекта (Vercel автоматически определит):
   - **Framework Preset**: Other
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
5. Нажмите **"Deploy"**
6. Готово! ✨

### Вариант 2: Через Vercel CLI

```bash
# Установить Vercel CLI
npm i -g vercel

# Логин
vercel login

# Деплой
cd /home/user/webapp
vercel --prod
```

## ⚠️ Важно для Vercel

Проект использует **Cloudflare Workers** runtime, который отличается от Node.js. 

Для деплоя на Vercel нужно переключиться на **Node.js** версию:

### Альтернатива: Деплой на Cloudflare Pages

Так как проект изначально создан для Cloudflare Workers, рекомендую использовать **Cloudflare Pages**:

```bash
# Установить Wrangler
npm install -g wrangler

# Логин в Cloudflare
wrangler login

# Создать проект
wrangler pages project create webapp --production-branch main

# Деплой
npm run build
wrangler pages deploy dist --project-name webapp
```

## 🔧 Настройка для Vercel (если нужен именно Vercel)

Если вы хотите использовать именно Vercel, нужно:

1. Создать `api/index.js` для Vercel Serverless Functions
2. Переписать роуты на Node.js runtime
3. Изменить архитектуру на Express/Next.js

Но проще всего - использовать **Cloudflare Pages**, для которого проект уже готов! 🎉

## 📱 Доступные платформы для деплоя

✅ **Cloudflare Pages** (рекомендуется) - проект готов  
⚠️ **Vercel** - нужна адаптация под Node.js  
⚠️ **Netlify** - нужна адаптация  
✅ **GitHub Pages** - можно для статики, но без API  

## 🌟 Рекомендация

Используйте **Cloudflare Pages** - проект уже полностью готов и оптимизирован для этой платформы!

```bash
# Быстрый деплой на Cloudflare Pages
npm run deploy
```

Ваш сайт будет доступен по адресу: `https://webapp.pages.dev`
